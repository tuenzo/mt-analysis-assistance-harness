## Tasks

- [x] **T1.1** — 创建 `.env.example` 模板文件，包含所有可配置项
- [x] **T1.2** — 创建 `.gitignore` 文件，忽略 `.env`、数据库、日志等
- [x] **T1.3** — 在 `config.py` 中集成 `python-dotenv` 的 `load_dotenv()`
- [x] **T1.4** — 更新 `Settings` 类添加 `env_file` 配置
- [x] **T1.5** — 更新 `CLAUDE.md` 说明 `.env` 文件的使用方法
