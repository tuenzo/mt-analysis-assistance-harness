## Tasks

- [x] Add a real-result dashboard adapter that reads panel summary, diagnostics, LocalGap, and recommended actions from workspace JSON artifacts.
- [x] Update the normal project dashboard route to fetch `.analysis/latest_result.json` and `.analysis/panel_summary.json`.
- [x] Keep the Keemart showcase dashboard on its isolated demo data path.
- [x] Update Agent side panel context and KPI snapshot to reuse the real dashboard summary.
- [x] Remove misleading hard-coded demo values from dashboard drilldowns, chart hints, artifact previews, header fallback, and campaign snapshot helpers.
- [x] Show pending/empty states when real artifacts are absent instead of rendering demo snapshot data.
- [x] Run frontend lint and build.
- [x] Browser-check real project Dashboard, Agent, and Reports pages for real dates/categories/KPIs and absence of old demo values.
